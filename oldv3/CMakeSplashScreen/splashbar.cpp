#include <QString>

// Wrapper for status text and bars if needed for complex animations
void UpdateSplashStatus(const QString &status, int progress) {
    // Implementation handled inside splashscreenlogic for simplicity
}
